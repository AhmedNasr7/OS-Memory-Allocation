def firt_fit (slef ,segment_list ,hole_list,memory_size): 
    for n in range (len(segments_list)):
            temp_hole_list=copy.deepcopy(hole_list)
            segements=0
            for j in range (len(segements_list[n])):

             for k in range (len(temp_hole_list)):
                 if(segements_l)